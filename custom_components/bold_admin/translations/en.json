{
  "config": {
    "step": {
      "user": {
        "title": "Bold Admin",
        "description": "Paste a freshly captured Bold refresh token and the BoldApp client secret. The token is spent immediately to verify it, and the rotated replacement is what gets stored. See the README for how to capture one.",
        "data": {
          "refresh_token": "Refresh token",
          "client_secret": "Client secret"
        }
      }
    },
    "error": {
      "invalid_auth": "Bold refused that token. It has probably expired, capture a fresh one via the browser login."
    },
    "abort": {
      "already_configured": "This Bold account is already set up."
    }
  }
}
